# 리스트에 보관되는 값은 "연관성"을 기반으로 보관한다.
# - 연관성은 굉장히 주관적이기 때문에 자유롭다.
# 리스트에 보관되는 값에는 제한이 없음
# - 원하는 값을 원하는 수량만큼 원하는 종류로 넣을 수 있음
lst = [123, 456, 3.14, 9.12, "Apple", "Pie", True, False]
# 주의사항 : 실제 사용시에는....
#           되도록, 같은 종류의 값들만 넣어서 보관한다.

# 리스트 출력은 완성된 프로그램을 만들 때는 하면 안됨
# - 그 외에는 마음대로 출력해야 한다.
print("8개의 값이 저장된 리스트")
print(lst)